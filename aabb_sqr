#include <stdio.h>

int main()
{
    
	int s ;
	int i = 32;
	while (i >= 32 && i < 100)
	{
		s = i * i;
		if (s / 1000 == s % 1000 / 100 && s % 100 / 10 == s % 10)
			printf("%d\n", s);
		i++;
	}
	return 0;
}
